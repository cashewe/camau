# Domain docs

This is a single-context repository.

Before exploring or naming domain concepts:

- Read `spec/CONTEXT.md`.
- Read relevant ADRs under `docs/adr/` when that directory exists.
- Use the glossary’s canonical vocabulary.
- Surface conflicts with existing ADRs explicitly.

If a referenced domain file does not exist, proceed silently.
